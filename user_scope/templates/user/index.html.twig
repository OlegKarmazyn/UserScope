<h1>List of users</h1>
<ul>
    {% for user in users %}
        <li>
            {{ user.username }} - Роли: {{ user.roles|join(', ') }}
            <a href="{{ path('user_edit', {'id': user.id}) }}">Редактировать</a>
        </li>
    {% endfor %}
</ul>